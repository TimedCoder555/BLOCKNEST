// Build detail page — will load a single build from Firestore by :id.
// Left as a stub in Phase 1; wired up once Firebase is connected in Phase 2.
export default function Build() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-20">
      <h1 className="font-display text-3xl text-nest-teal">Build page — coming in Phase 2</h1>
    </main>
  )
}
