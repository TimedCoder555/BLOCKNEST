// Admin dashboard — gated behind Firebase Auth + Firestore "admin" role.
// Left as a stub in Phase 1; real auth flow and CRUD tools land in Phase 3.
export default function Admin() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-20">
      <h1 className="font-display text-3xl text-nest-teal">Admin dashboard — coming in Phase 3</h1>
    </main>
  )
}
